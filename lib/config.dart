// API ENDPOINT CONFIGURATION
const endpoint = 'http://localhost:8000/';

const api = {
  "login": "${endpoint}api/token/",
  "usuarios": "${endpoint}usuarios/",
  "cursos": "${endpoint}cursos/",
  "registros": "${endpoint}registro/",
};
